import { StyledContainerMenu } from "./StyledContainerMenu"

export const ContainerMenu = (props:any) => {
    return(
        <StyledContainerMenu>
            
        </StyledContainerMenu>
    )
}